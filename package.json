{
  "name": "edu-tech-marketing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R apps/diagnostico-v1/. dist/",
    "qa:diagnostico": "node apps/diagnostico-v1/qa/simulate-300.mjs baseline"
  }
}

